import React from "react";
import { Button, Text, View, ViewScroll, SafeAreaView } from "react-native";

const Settings = () => {
  return (
    <SafeAreaView>
      <View style={{ margin: 30 }}>
        <Button title="Settings Page" />
      </View>
    </SafeAreaView>
  );
};

export default Settings;
